/**
 * Error message for invalid display settings
 */
export const DISPLAYS_HAVE_SEPARATE_SPACES = `Must set Apple menu > System Preferences > Mission Control > Displays have Separate Spaces`;

/**
 * App locations
 */
export const TERMINAL_BOTTOM = {
  left: 0,
  top: 0.4,
  right: 0,
  bottom: 0,
};
